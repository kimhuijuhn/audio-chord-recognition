repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.15
    hooks:
      # Run the linter (with auto-fix).
      - id: ruff-check
        args: [--fix]
      # Run the formatter.
      - id: ruff-format